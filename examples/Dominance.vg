// Dominance: A always can get more regardless of B's choices (test case for strategy dominance)

game main() {
  join A() $ 10;
  join B() $ 10;
  yield or split A(c: bool) B(c: bool);
  withdraw (A.c)
      ? { A -> 15; B -> 5 }
      : { A -> 5; B -> 15 }
}
